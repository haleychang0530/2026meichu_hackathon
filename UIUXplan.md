# hear tAIgi 前端完整視覺與 UX 重設計

## 摘要

以暖色品牌視覺將目前偏工程驗證台的介面改造成高對比、Audio-first 的臺語教學產品。保留現有 React、Adapter、session、SSE、半雙工語音與 student-safe selector，不重寫後端業務邏輯。

優先修正：

- 待審核 Lesson 直接建立 session，會觸發 `LESSON_NOT_APPROVED`。
- 學生必須先讀完整服務健康資訊才能抵達課程。
- 首用朗讀對話框沒有正確接管／限制焦點。
- `STAGE`、`MOCK`、`Core Backend`、revision 等工程文字進入正式介面。
- 前端自行把所有提示包成 `zh-TW / needs_review / prerecorded`，無法正確路由台語教學語音。
- 教材擷取、分析結果與 session 建立混在同一長頁，操作階段不清楚。

## 資訊架構與畫面

- 建立兩種 Shell：
  - 學生 Shell：只保留課程、進度、主要操作和求助入口，不顯示全域導覽、模型或服務資訊。
  - 教師／家長 Shell：提供首頁、教材、觀察、系統狀態與朗讀設定。
- 首頁改為產品入口：
  - 主標題：「讓一頁台語教材，變成可以聽、可以說的課程」。
  - 主要操作：「準備一頁教材」。
  - 有有效 session 時顯示「繼續目前課程」。
  - 朗讀與顯示設定為次要操作；Mock／fixture 工具只在明確的 demo flag 下出現。
  - 「體驗範例課程」與「準備一頁教材」使用相同字級。
  - 使用步驟放在單一白底圓角大框內，由上而下列出 01／02／03；序號和步驟標題使用 `#8A5B00` 與較大字體，說明文字維持原字級。
- 教材流程改為四步驟：
  1. 拍攝或選擇教材。
  2. 檢查照片品質。
  3. 分析中，提供進度、取消與離線備援。
  4. 教師審核解析內容、citation 與無障礙活動。
- 新增 `/lessons/{lessonId}/review`：
  - 使用既有 GET／PATCH Lesson API。
  - 核准成功後才呼叫 `POST /api/sessions`。
  - 收到 `LESSON_NOT_APPROVED` 時返回審核畫面並保留編輯內容。
- 學生模式改成狀態驅動的單一主要操作：
  - 示範階段：「聽完整課文」。
  - 跟讀階段：「開始跟讀」。
  - 錄音中：「停止並查看回答」。
  - 有逐字稿後：「送出回答」。
  - 評估中只顯示處理狀態；完成後顯示「完成課程」。
  - 提示、鍵盤回答、暫停放在次要操作區；逐字稿只在開始回答後展開。
  - 移除完整 `StatusBanner`、同步游標與技術名稱，只在降級時顯示一則可行動提示。
- 教師／家長模式分為：
  - 即時觀察：目前階段、最新回答、回饋與教師控制。
  - 教材內容：課文、詞彙、臺羅、學習目標與引用。
  - 課後摘要：提示歷程、熟悉度與待複習項目。
  - 系統狀態：預設收合；模型 revision、queue、request ID 放入「技術資訊」。
- 首次朗讀選擇改用真正的 modal focus management：
  - 開啟時將背景設為 inert，焦點移入對話框並限制 Tab。
  - 明確選擇「使用 Narrator／NVDA」或「使用內建旁白」。
  - 路由焦點移動必須等設定完成後才執行。
  - 內建旁白控制縮成可展開工具列，但「停止朗讀」隨時可到達。

## 視覺系統與介面契約

- 產品顯示名稱為 `hear tAIgi`；左上角使用工作資料夾最新版 `basic_icon.png`，圖示保留圓角，並搭配文字名稱。
- 導覽列不另加「目前頁面」文字；目前所在項目以 `#FFE8C4` 底色標示，不加深棕色底線。導覽項目的底色塊貼近文字，但維持足夠的點選高度。
- 色彩：
  - 基本圖示保留檔案原色；介面有色填滿區塊使用 `#FFCC7A`，原本白色卡片仍維持白色。
  - 深色文字：`#2A210F`。
  - 暖色背景：`#FFF8E8`。
  - 卡片：`#FFFFFF`。
  - 輔助藍綠：`#135C5A`。
  - 深琥珀文字／邊界：`#8A5B00`。
  - 危險操作：`#9F2D20`。
- 內容區有色連結／按鈕使用 `#FFE8C4` 底色與深色文字，白底次要按鈕保留白色；縮小文字四周的內距，但主要操作仍至少 48px 高。可點選方框取消外框與圓角，改用 `#2A210F` 深棕色底線；導覽列項目不套用這個方框規則。
- 準備教材頁面中有底色的說明文字統一使用 `#FFE8C4`；相機控制列（開始預覽、停止預覽等）按鈕不使用深棕色底線。
- 準備教材頁第一個「拍攝或選擇教材」區塊的上框線維持原色，改為細線。各流程區塊頂端以 `01 · 拍攝或選擇教材`、`02 · 確認照片`、`03 · 查看教材` 顯示數字與文字說明，不顯示工程 Stage 名稱。三個流程大方框保留圓角；區塊內有底色說明改為直角，其左側輔助粗線統一使用 `#C08000`。
- Focus 使用深色內框加琥珀外圈，確保在白色、暖色與深色背景都清楚。
- 所有主要操作至少 48px 高；支援 200%–400% reflow、forced-colors 與 320px 寬度。
- 非互動卡片保留柔和圓角，並使用教材頁面與聲音波紋的 CSS／SVG 裝飾；裝飾一律 `aria-hidden`，圖示永遠搭配文字。
- 加入 skip link、動態 `document.title`、一致的頁面標題焦點與單一主要 live region。
- 學生錯誤只說明問題與下一步；錯誤碼和 fallback metadata 只在教師技術資訊中展開。

公開介面調整：

- 保留既有 `current_prompt` 作為可見文字。
- 以向後相容的 optional 欄位，為 Session／StudentAction／TurnResult 增加經 Backend 正規化的 `Utterance`，讓前端直接交給 Speech Gateway；前端不得再自行猜測 `lang` 或建立 `needs_review` 語音。
- OpenAPI 版本更新為 `0.1.1`，同步 canonical schema、fixtures、契約測試與 generated TypeScript types。
- Student payload 仍禁止 evidence、confidence、answer evidence、review status 與教師控制資料。
- React／Speech 改動由 Agent B branch 實作；Agent A 先完成語音契約與 handoff，不直接修改 Agent B 核心畫面。

## 驗證與驗收

- 保留目前基線：29 個前端測試、typecheck、production build 均須持續通過。
- 新增元件與端對端測試：
  - pending Lesson 無法略過審核，核准後才能建立 session。
  - `LESSON_NOT_APPROVED` 能無資料遺失地返回審核。
  - modal 初始焦點、焦點圈限、背景 inert 與設定後回焦。
  - 每種學生狀態只出現一個明確主要操作。
  - 學生 DOM、store、cache、SSE 與錯誤訊息無教師專用欄位。
  - 台語／中文 `Utterance` 路由正確，播放、旁白與錄音不重疊。
- 使用 axe 與瀏覽器 E2E 檢查首頁、拍攝、審核、學生、教師及錯誤狀態，不得有 critical／serious 問題。
- 在 320px、768px、1440px、200% 與 400% 縮放下驗證無內容裁切及頁面雙向捲動。
- 純鍵盤、Narrator、NVDA、App 旁白各完成一次：
  - 開啟系統 → 拍攝教材 → 核准 → 聽課文 → 跟讀／鍵盤回答 → 取得回饋 → 完成。
- 完成 20 次學生／教師模式切換、重新整理恢復、MI300 離線、ASR／TTS 失敗與相機拒絕情境。
- 正式封版前至少安排一位實際螢幕閱讀器使用者完成核心流程並記錄阻塞點。

## 假設

- 採完整視覺重設計，介面有色填滿區塊使用 `#FFCC7A`，目前導覽項目使用 `#FFE8C4`。
- 目標仍是 Ryzen AI 9 筆電上的單頁教材、單一學生、3–5 分鐘 MVP。
- 不新增帳號、正式權限、多學生或雲端同步。
- Mock 與技術診斷保留，但不進入一般學生的主要閱讀順序。
- UI branch 必須從包含 Agent A Stage 08 source-read／approval gate 的核准整合基線建立，不直接在目前 Agent A feature branch 上修改前端。
